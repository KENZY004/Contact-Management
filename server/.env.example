# MongoDB Atlas Connection String
# Replace <username>, <password>, and <cluster-url> with your actual credentials
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/contact-management?retryWrites=true&w=majority

# Server Port
PORT=5000

# Environment
NODE_ENV=production
